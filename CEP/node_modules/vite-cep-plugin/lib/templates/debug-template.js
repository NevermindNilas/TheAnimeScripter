"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.debugTemplate = void 0;
const debugTemplate = (props) => {
    const { id, hosts, startingDebugPort, panels } = props;
    let port = startingDebugPort;
    return `
<?xml version="1.0" encoding="UTF-8"?>
<ExtensionList>${panels
        .map(({ id }) => `<Extension Id="${id}">
      <HostList>
      ${hosts
        .map((host) => `<Host Name="${host.name}" Port="${port++}"/>`)
        .join("")}
      </HostList>
      </Extension>`)
        .join("")}
</ExtensionList>
`;
};
exports.debugTemplate = debugTemplate;
