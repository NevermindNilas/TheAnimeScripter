# This project was generated with 0.3.19 using template: https://github.com/beeware/briefcase-template@v0.3.19
[tool.briefcase]
project_name = "TheAnimeScripter"
bundle = "com.theanimescripter"
version = "0.0.1"
url = "https://github.com/NevermindNilas/TheAnimeScripter"
license.file = "LICENSE"
author = "https://github.com/NevermindNilas/TheAnimeScripter"
author_email = "None"

[tool.briefcase.app.theanimescripter]
formal_name = "TheAnimeScripter"
description = "the ultimate tool for Video Upscaling, Interpolating and many more. Available as a CLI, GUI and Adobe Extension."
long_description = """More details about the app should go here.
"""
sources = ["theanimescripter"]
test_sources = ["tests"]

console_app = true
requires = [
    "torch==2.3.1",
    "torchvision==0.18.1",
    "opencv-python-headless",
    "timm",
    "numpy==1.26.4",
    "kornia",
    "scipy",
    "wmi",
    "spandrel",
    "yt-dlp",
    "requests",
    "alive-progress",
    "scikit-image",
    "scenedetect[opencv]",
    "onnxruntime-directml",
    "pytorch-lightning",
    "inquirer",
    "colored",
    "vidgear",
    "flask",
    "tensorrt",
    "tensorrt_cu12",
    "tensorrt-cu12-bindings",
    "tensorrt-cu12_libs",
    "https://github.com/NevermindNilas/TAS-Modes-Host/releases/download/main/rife_ncnn_vulkan_python-1.2.1-cp312-cp312-win_amd64.whl",
    "https://github.com/NevermindNilas/TAS-Modes-Host/releases/download/main/upscale_ncnn_py-1.2.0-cp312-none-win_amd64.whl",
    "https://github.com/NevermindNilas/TAS-Modes-Host/releases/download/main/polygraphy-0.49.13-py2.py3-none-any.whl",
]
test_requires = ["pytest"]

[tool.briefcase.app.theanimescripter.macOS]
universal_build = true
requires = []

[tool.briefcase.app.theanimescripter.linux]
requires = []

[tool.briefcase.app.theanimescripter.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.theanimescripter.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.theanimescripter.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.theanimescripter.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.theanimescripter.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "23.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.theanimescripter.windows]
requires = []

# Mobile deployments
[tool.briefcase.app.theanimescripter.iOS]
supported = false

[tool.briefcase.app.theanimescripter.android]
supported = false

# Web deployments
[tool.briefcase.app.theanimescripter.web]
supported = false
