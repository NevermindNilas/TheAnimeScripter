# Linux ROCm (AMD HIP) profile — experimental.
# Mirrors extra-requirements-linux.txt but swaps the CUDA torch wheels for the
# ROCm builds and drops NVIDIA-only packages (TensorRT, Maxine nvidia-vfx).
# Requires an AMD GPU with ROCm drivers; -tensorrt / maxine-* methods stay
# NVIDIA-only and will fail on this profile. Use the -rocm method suffixes
# (e.g. --upscale_method shufflecugan-rocm) for the eager HIP paths.
--extra-index-url https://download.pytorch.org/whl/rocm6.4
torch==2.14.0+rocm6.4
torchvision==0.29.0+rocm6.4
# Keep the native video wheel on the same PyTorch ABI during profile installs.
nelux==0.19.0
opencv-python-headless==5.0.0.93
timm==1.0.29
onnx==1.22.0
onnxruntime
safetensors==0.8.0
scenedetect[opencv]
yt-dlp==2026.8.19
vmaf-torch
https://github.com/NevermindNilas/TAS-Models-Host/releases/download/main/rife_ncnn_vulkan_python_tntwise-1.4.5-cp314-cp314-manylinux1_x86_64.whl
